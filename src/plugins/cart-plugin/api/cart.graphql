# src/plugins/cart-plugin/api/cart.graphql
extend type Query {
  customActiveOrder: Order
}

extend type Mutation {
  customAddItemToOrder(productVariantId: ID!, quantity: Int!): Order!
  customAdjustOrderLine(orderID: ID!, orderLineId: ID!, quantity: Int!): Order!
  customDeleteOrderLine(orderID: ID!, orderLineId: ID!): Order!
}
